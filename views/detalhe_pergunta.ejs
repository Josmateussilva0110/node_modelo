<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%=pergunta.titulo%></h1>
        <p><%=pergunta.descricao%></p>
        <hr>
        <a class="bt-listar-perguntas" href="/editar_pergunta/<%= id%>">Editar</a>
        <a class="bt-listar-perguntas" href="/home">Lista de Perguntas</a>
        <form action="/deletar/<%= pergunta.id %>" method="POST" onsubmit="return confirm('Deseja deletar esta pergunta?')">
            <button type="submit" class="btn-deletar">Deletar</button>
        </form>
        
    </div>
</body>
</html>